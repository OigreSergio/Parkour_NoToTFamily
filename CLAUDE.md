@AGENTS.md

Note per Claude Code in particolare:

- Rispondi e scrivi in italiano; il codice segue le convenzioni del punto 3 di AGENTS.md.
- Prima di modificare `remote-service/` esegui `ruff check .` e `pytest` in quella cartella e riportane l'esito nella nota di consegna.
- Non aprire pull request, non fare push su `main` o `gh-pages`, non toccare il progetto Supabase di produzione: sono azioni di una persona.
- Usa i sotto-agenti solo per indagini ampie su molti file, al massimo quattro in parallelo.
